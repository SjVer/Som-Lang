-- main =
--   -- apply f x = f x =>
--   -- int = 0 =>
--   apply this
--   .

main = f x
---
[prog:
  [list(toplevel):
    [toplevel:
      [definition:
        [binding(expr):
          [pattern: [simple_pattern: LOWERNAME]]
          [strict_binding(expr): EQUAL [expr: error]]
        ]
      ]
      [dot: error]
    ]
    [list(toplevel):]
  ]
  EOF
]

prog -> list(toplevel) -> toplevel -> definition -> binding(expr) -> pattern -> simple_pattern -> LOWERNAME
     |                 |           |                              -> strict_binding(expr) -> EQUAL
     |                 |           |                                                      -> expr -> error
     |                 |           -> dot -> error
     |                 -> list(toplevel) -> []
     -> EOF
---