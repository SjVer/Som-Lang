#std::io::puts
#std::str::show

-- forward decl
fib: int -> int

main args =
	xs = [1, 2, 3, 4, 5]
	->
		puts show map xs fib,
		-- prints "[0, 1, 1, 2, 3]"

		0

fib n = n ??
	0 -> 0,
	1 -> 1,
	_ -> fib (n - 1) + fib (n - 2)